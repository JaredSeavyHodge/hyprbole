#!/bin/bash

# hyprbole:summary=Apply configured font roles to runtime configs

set -euo pipefail

source "${HYPRBOLE_PATH:-$HOME/.local/share/hyprbole}/bin/hyprbole-lib"

hyprbole_apply_font_settings
