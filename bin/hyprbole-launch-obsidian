#!/bin/bash

# hyprbole:summary=Launch Obsidian or install the Obsidian extra

set -euo pipefail

if ! command -v obsidian >/dev/null 2>&1; then
  exec hyprbole launch extra-install obsidian
fi

exec uwsm-app -- obsidian "$@"
